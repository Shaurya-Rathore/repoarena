# RepoArena CLI

RepoArena is CI for AI coding agents. Install the CLI with Node.js 24:

```sh
npm install --global repoarena
repoarena --version
```

See the project README and documentation for initialization, deterministic
repository readiness, benchmarks, reports, optimization, and the local UI.
