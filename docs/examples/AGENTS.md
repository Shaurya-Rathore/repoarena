# Example agent guidance

Run the repository's focused tests before the complete suite. Keep changes
small, preserve public APIs, and never read `.repoarena/state/private`.

This is an example candidate, not a universal optimum. Compare guidance variants
with `repoarena optimize` on repository-specific tasks.
