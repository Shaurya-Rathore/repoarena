# Implementation specification

The project is built against the RepoArena implementation bible supplied to the
maintainers. This index intentionally avoids duplicating evaluator-only content.
Key local-first contracts are implemented in `packages/task-spec`,
`packages/runner-core`, `packages/readiness`, and `packages/cli`.
