# Intended atomic commits

| Title | Files changed | Reason | Tests passed |
| --- | --- | --- | --- |
| `chore(repo): establish local-first foundation` | root tooling, core/config/task/runner/reporter/readiness/CLI | Initial working baseline | `pnpm verify` |
| `docs(development): add implementation ledger and Codex workflows` | `docs/development`, `.agents/skills`, `scripts/validate-skills.mjs` | Persistent execution status and recurring operating knowledge | `pnpm skills:validate` |
| `feat(git): add deterministic repository boundary` | `packages/git`, lockfile | Safe argv-only Git APIs and temporary repository fixtures | `pnpm --filter @repoarena/git test:unit` |
| `feat(tasks): add historical task mining foundation` | task spec, discovery, reconstruction, leakage, validation, CLI | Canonical task v1 and initial historical task workflow | `pnpm verify` |
| `feat(execution): add sandbox providers and redaction` | sandbox-local, sandbox-docker, redaction | Isolated command execution, Docker contract, secret redaction | focused sandbox/redaction tests |
| `feat(evaluator): add behavior-based result boundary` | `packages/evaluator`, lockfile, implementation ledger | Decide from public/private behavioral evidence and prevent private command evidence from entering public result structures | `pnpm --filter @repoarena/evaluator build && pnpm --filter @repoarena/evaluator test:unit && pnpm --filter @repoarena/evaluator typecheck` |
| `feat(artifacts): add safe manifest collector` | `packages/artifacts`, lockfile, implementation ledger | Constrain agent-declared artifacts to regular files under an attempt root | `pnpm --filter @repoarena/artifacts build && pnpm --filter @repoarena/artifacts test:unit && pnpm --filter @repoarena/artifacts typecheck` |
| `feat(integrity): add deterministic change policy` | `packages/integrity`, lockfile, implementation ledger | Detect protected-path edits, test deletion, and verification tampering from Git metadata | `pnpm --filter @repoarena/integrity build && pnpm --filter @repoarena/integrity test:unit && pnpm --filter @repoarena/integrity typecheck` |
| `feat(runner): orchestrate isolated private verification` | `packages/runner-core`, `packages/git`, evaluator dependency, lockfile | Capture agent modifications before a distinct private evaluator workspace is created | `pnpm --filter @repoarena/runner-core test:unit && pnpm --filter @repoarena/runner-core typecheck` |
| `feat(results): add atomic public run persistence` | `packages/run-store`, lockfile, implementation ledger | Persist canonical result state with fsync and atomic replacement without private evaluator types | `pnpm --filter @repoarena/run-store build && pnpm --filter @repoarena/run-store test:unit && pnpm --filter @repoarena/run-store typecheck` |
| `feat(pricing): persist immutable cost snapshots` | `packages/pricing`, implementation ledger | Select effective versioned prices once and preserve unknown/partial usage states | `pnpm --filter @repoarena/pricing build && pnpm --filter @repoarena/pricing test:unit && pnpm --filter @repoarena/pricing typecheck` |
| `feat(reports): render canonical public run formats` | `packages/reporter`, lockfile, implementation ledger | Generate terminal, JSON, standalone HTML, and JUnit from one public result truth | `pnpm --filter @repoarena/reporter build && pnpm --filter @repoarena/reporter test:unit && pnpm --filter @repoarena/reporter typecheck` |
| `feat(benchmark): orchestrate repetitions and persistence` | `packages/benchmark-engine`, runner execution strategy, lockfile, implementation ledger | Run bounded clean attempts, persist incremental snapshots, aggregate actual outcomes and cost | `pnpm --filter @repoarena/benchmark-engine build && pnpm --filter @repoarena/benchmark-engine test:unit && pnpm --filter @repoarena/benchmark-engine typecheck` |
| `feat(cli): run connected benchmarks and reports` | CLI dependencies/commands/tests, runner workspace privacy, lockfile, implementation ledger | Use production adapter registry and benchmark engine; execute private hooks only in evaluator copies and export all public formats | `pnpm --filter repoarena build && pnpm --filter repoarena test:unit && pnpm --filter repoarena typecheck` |
| `feat(evaluator): wire regression and integrity outcomes` | evaluator, runner, benchmark/reporter types/tests, implementation ledger | Centralize final outcome precedence and reject test deletion through the connected lifecycle | focused evaluator, runner, benchmark-engine and reporter checks |
| `fix(runner): include untracked agent changes` | `packages/git`, `packages/runner-core` | Include added/binary/odd-named working-tree files using NUL-safe Git metadata | `pnpm --filter @repoarena/git test:unit && pnpm --filter @repoarena/runner-core test:unit` |
| `chore(execution): keep verification lint-clean` | recent execution packages | Resolve Biome correctness diagnostics surfaced by the full gate | `pnpm lint && pnpm typecheck` |
| `feat(execution): integrate safe artifacts, redaction and sandbox execution` | `packages/runner-core`, `packages/benchmark-engine`, `packages/run-store`, `packages/cli`, tests | Ensure allowlisted artifacts, observed adapter usage, argv execution policy and secret-safe patch/evidence flow into persisted attempts | runner, benchmark-engine and CLI unit suites; lint/typecheck |
| `docs(execution): record artifact and sandbox integration` | `docs/development/implementation-status.md` | Keep subsystem ledger aligned with the latest verified connected path and Docker environment limitation | `pnpm verify`; `pnpm skills:validate` |
| `feat(execution): separate provider and infrastructure retries` | `packages/benchmark-engine`, `packages/run-store`, tests | Persist typed retry events with independent budgets/backoff while preserving one logical statistical attempt | `pnpm --filter @repoarena/benchmark-engine test:unit`; typecheck |
| `test(execution): verify historical cost across report reload` | `packages/benchmark-engine`, `packages/reporter`, tests | Prove persisted V1 costs survive active catalog replacement and all public reporters use stored values | benchmark-engine connected cost reload test |
| `test(execution): add connected adversarial benchmark matrix` | `packages/benchmark-engine`, `packages/runner-core`, tests | Exercise perfect, wrong, no-op, partial hidden failure, and test deletion through real workspaces, verification, integrity, persistence and aggregation | `pnpm --filter @repoarena/benchmark-engine test:unit` |
