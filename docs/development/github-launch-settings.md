# GitHub launch settings

These are repository-owner recommendations only. No launch script mutates GitHub.

- Current repository: `Shaurya-Rathore/repoarena`.
- Current visibility: private. **HUMAN EXECUTION REQUIRED:** choose public at
  launch only after reviewing the final tree, history, security scan, and
  package artifacts. Keeping it private means there is no OSS launch.
- Default branch: keep `main`; fast-forward it from the approved
  `release/oss-launch` tip using the launch runbook.
- Description: `CI for AI coding agents. Benchmark Codex, Claude Code, Gemini CLI, and more on real bugs from your repo.`
- Topics: `ai`, `coding-agents`, `benchmark`, `evals`, `codex`, `claude-code`, `gemini`, `developer-tools`, `ci`, `llm`
- Issues are currently enabled; retain them and use the repository issue forms.
- Enable GitHub private vulnerability reporting before making the repository
  public. The project deliberately does not invent a security email address.
- Discussions are currently disabled. Enable them only if maintainers intend to
  respond; Issues are sufficient for the initial launch.
- **HUMAN EXECUTION REQUIRED:** upload
  [`social-preview.svg`](../assets/social-preview.svg) as the social preview
  after reviewing its rendered output.
- Add the topics above and verify the README, license, issue forms, and Security
  tab are visible after changing visibility.

The package metadata, Action examples, issue forms, and launch notes use the
current permanent identity `Shaurya-Rathore/repoarena`. If the repository is
transferred later, update and revalidate those public references before the
next release.
